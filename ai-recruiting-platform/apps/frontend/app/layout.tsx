import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'AI Recruiting Platform',
  description: 'Production-grade AI recruiting platform with automated sourcing, scoring, and outreach',
  keywords: ['recruiting', 'AI', 'hiring', 'HR', 'talent acquisition'],
  authors: [{ name: 'AI Recruiting Platform Team' }],
  viewport: 'width=device-width, initial-scale=1',
  themeColor: '#3b82f6',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}